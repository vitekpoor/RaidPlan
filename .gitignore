__pycache__/
# contains RaidPlan.io edit keys (grant edit access) - do not publish
tools/raidplan/venomabyss_plans.txt
# pre-save snapshots written by raidplan.py --in-place / --boss
tools/raidplan/backup_*.json
# ES Attendance addon: sync state + optional private config (web app token)
addon/.synced.json
addon/es_attendance.config.json

# Flopik: private config (web app token) + local per-pull log
tools/flopik/flopik.config.json
tools/flopik/pulls.jsonl
